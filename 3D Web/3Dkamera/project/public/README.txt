Place the camera showcase video file here as `kamera.mp4`.

The hero section of the landing page references `/kamera.mp4` and drives its playback position based on page scroll progress. Without this file the hero will show a loading state; the rest of the marketing page (features, specs, accessories, pre-order, footer) works independently.
